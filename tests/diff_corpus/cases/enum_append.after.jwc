// ops: 1 add_enum_value org.plan 'enterprise'

namespace c;
database App : Postgres;
schema org of App;

enum Plan of App.org { free, pro, enterprise }

table Orgs of App.org {
    id   bigint primary key identity;
    slug varchar(40) unique;
    plan Plan;
    name varchar(80)?;
}
