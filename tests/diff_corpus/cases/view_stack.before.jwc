namespace c;
database App : Postgres;
schema org of App;

table Orgs of App.org {
    id   bigint primary key identity;
    slug varchar(40);
    name varchar(80)?;
}

view OrgSummary of App.org {
    select O from App.org.Orgs as { O.id, O.slug };
}

view OrgTop of App.org {
    select S from App.org.OrgSummary as { S.id, S.slug };
}
