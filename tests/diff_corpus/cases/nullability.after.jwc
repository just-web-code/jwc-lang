// ops: 2 set_not_null org.orgs.name

namespace c;
database App : Postgres;
schema org of App;

enum Plan of App.org { free, pro }

table Orgs of App.org {
    id   bigint primary key identity;
    slug varchar(40) unique;
    plan Plan;
    name varchar(80) default "x";
}
