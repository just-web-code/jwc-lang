// ops: 1 add_enum_value org.plan 'team' before 'pro'

namespace c;
database App : Postgres;
schema org of App;

enum Plan of App.org { free, team, pro }

table Orgs of App.org {
    id   bigint primary key identity;
    slug varchar(40) unique;
    plan Plan;
    name varchar(80)?;
}
