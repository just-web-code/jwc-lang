// ops: 2 rename_column org.orgs.name -> display_name
// ops: 2 alter_column_type org.orgs.display_name varchar(80) -> varchar(200)
// diag: E1104

namespace c;
database App : Postgres;
schema org of App;

enum Plan of App.org { free, pro }

table Orgs of App.org {
    id   bigint primary key identity;
    slug varchar(40) unique;
    plan Plan;
    display_name varchar(200)? was "name";
}
