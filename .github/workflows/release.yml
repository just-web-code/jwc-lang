name: Release jwc binaries

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Override version label (default: tag name / dev-<sha>)'
        required: false

permissions:
  contents: write

jobs:
  # The tag names the asset; Cargo.toml names what the binary reports. Nothing
  # tied the two together, so `git tag v0.9.910` on a tree at 0.9.913 would
  # publish `jwc-v0.9.910-x86_64-linux.tar.gz` containing a binary that prints
  # `jwc 0.9.913` — and the mismatch only surfaces once someone is trying to
  # work out which version they are running.
  #
  # Its own job so it fails before five build matrices spend their minutes.
  version:
    name: Tag matches Cargo.toml
    if: github.ref_type == 'tag'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Compare
        shell: bash
        run: |
          tag="${GITHUB_REF_NAME#v}"
          crate=$(sed -n '/^\[package\]/,/^\[/ s/^version *= *"\(.*\)"/\1/p' Cargo.toml | head -1)
          echo "tag=$tag  Cargo.toml=$crate"
          if [[ "$tag" != "$crate" ]]; then
            echo "::error::tag v$tag does not match Cargo.toml version $crate."
            echo "::error::Bump Cargo.toml (and Cargo.lock) or retag, then push again."
            exit 1
          fi

  build:
    name: Build ${{ matrix.target }}
    # `workflow_dispatch` skips the guard above, and a job that `needs` a
    # skipped job is skipped too unless it says otherwise.
    needs: version
    if: always() && needs.version.result != 'failure'
    strategy:
      fail-fast: false
      matrix:
        include:
          # glibc builds pin the OLDEST supported runner on purpose. A glibc
          # binary runs on its build glibc or newer, never older, and
          # `ubuntu-latest` moved to 24.04 — so v0.9.6 shipped binaries
          # requiring GLIBC_2.39 and they refused to start on Ubuntu 22.04
          # (2.35), Debian 12 (2.36), RHEL 9 / Amazon Linux 2023 (2.34):
          #
          #   jwc: /lib/.../libc.so.6: version `GLIBC_2.39' not found
          #
          # 22.04 gives glibc 2.35 and covers all of those. Do not "upgrade"
          # these to ubuntu-latest — that silently drops distros. Anything
          # older than 2.35 is served by the musl builds below, which carry
          # no libc dependency at all.
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-22.04
            short: x86_64-linux
            ext: tar.gz
          # musl links libc statically, so the runner's glibc is irrelevant
          # here; these stay on the current image.
          - target: x86_64-unknown-linux-musl
            os: ubuntu-latest
            short: x86_64-unknown-linux-musl
            ext: tar.gz
          # arm64 Linux, built on GitHub's native ARM runners (free for public
          # repos). Native, not cross-compiled — the QEMU route is what made
          # the Docker arm64 leg hang for 30+ minutes before it was dropped.
          # Covers Raspberry Pi / Ampere / Graviton hosts and Android shells,
          # where the installer used to dead-end on "Unsupported platform".
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-22.04-arm
            short: aarch64-linux
            ext: tar.gz
          - target: aarch64-unknown-linux-musl
            os: ubuntu-24.04-arm
            short: aarch64-unknown-linux-musl
            ext: tar.gz
          # macOS has never been built — not deleted at any cutover, never
          # in this matrix at all, while the install page claimed archives
          # for it. GitHub's macOS runners are free for public repos, so
          # the reason was an oversight rather than a cost.
          #
          # Both run on macos-14 (arm64). The arm64 target builds natively;
          # the x86_64 one is cross-compiled, which on macOS needs nothing
          # but the target's std — clang ships every arch and the SDK is
          # universal.
          #
          # x86_64 was on `macos-13`, the last Intel image, for one release.
          # In v0.9.925 that job sat queued for 25 minutes while all six
          # other targets finished in two and attached their archives, so
          # the tag shipped without an Intel Mac binary and `install.sh`'s
          # `darwin-x86_64` case pointed at an asset that was not there.
          # GitHub has been retiring the Intel images; a runner label that
          # may or may not have capacity is not something a release can be
          # made to depend on.
          - target: x86_64-apple-darwin
            os: macos-14
            short: x86_64-macos
            ext: tar.gz
          - target: aarch64-apple-darwin
            os: macos-14
            short: aarch64-macos
            ext: tar.gz
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            short: x86_64-windows
            ext: zip
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      # Matches both musl targets. Pinning the x86_64 spelling silently
      # skipped this on aarch64-unknown-linux-musl, which then failed to link.
      - name: Install musl toolchain (musl targets only)
        if: endsWith(matrix.target, '-linux-musl')
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends musl-tools

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Compute version label
        id: ver
        shell: bash
        run: |
          if [[ "${{ github.ref_type }}" == "tag" ]]; then
            echo "label=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
          elif [[ -n "${{ inputs.version }}" ]]; then
            echo "label=${{ inputs.version }}" >> "$GITHUB_OUTPUT"
          else
            echo "label=dev-$(echo ${{ github.sha }} | cut -c1-7)" >> "$GITHUB_OUTPUT"
          fi

      # `--features redis` is what puts the redis_* driver in the shipped
      # binary. Without it every release prints "built without Redis
      # support" at boot when JWC_REDIS_URL is set, and the built-ins fail
      # — the feature exists in the tree but reaches nobody.
      #
      # Safe across the matrix: the feature's TLS is rustls with bundled
      # webpki roots, and `ring` is already a direct dependency, so musl
      # gains no new C toolchain requirement.
      - name: Build
        run: cargo build --release --features redis --bin jwc --target ${{ matrix.target }}

      # `-x` on macOS: plain `strip` there removes symbols ld needs and
      # the result will not run. Linux takes the default.
      - name: Strip (unix)
        if: runner.os != 'Windows'
        run: |
          if [ "${{ runner.os }}" = "macOS" ]; then
            strip -x target/${{ matrix.target }}/release/jwc
          else
            strip target/${{ matrix.target }}/release/jwc
          fi

      # macOS as well as Linux. Both were gated on `runner.os == 'Linux'`
      # when the darwin targets were added, which would have produced two
      # green build jobs and no assets — a release that looks complete and
      # is missing a platform.
      #
      # `shasum -a 256` because macOS has no `sha256sum`; its output is the
      # same "<hash>  <file>" that `sha256sum -c` reads.
      - name: Package (unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          mkdir -p stage
          cp target/${{ matrix.target }}/release/jwc stage/
          cp README.md ROADMAP.md stage/
          asset="jwc-${{ steps.ver.outputs.label }}-${{ matrix.short }}.tar.gz"
          tar -czf "$asset" -C stage .
          if command -v sha256sum >/dev/null 2>&1; then
            sha256sum "$asset" > "$asset.sha256"
          else
            shasum -a 256 "$asset" > "$asset.sha256"
          fi
          echo "ASSET=$asset" >> "$GITHUB_ENV"

      - name: Package (windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $stage = New-Item -ItemType Directory -Path stage -Force
          Copy-Item "target/${{ matrix.target }}/release/jwc.exe"    "$stage/"
          Copy-Item README.md, ROADMAP.md "$stage/"
          $asset = "jwc-${{ steps.ver.outputs.label }}-${{ matrix.short }}.zip"
          Compress-Archive -Path "$stage/*" -DestinationPath $asset -Force
          $hash = (Get-FileHash $asset -Algorithm SHA256).Hash.ToLower()
          # `sha256sum -c` compatible format: "<hash>  <filename>"
          Set-Content -Path "$asset.sha256" -Value "$hash  $asset" -NoNewline -Encoding ascii
          Add-Content -Path $env:GITHUB_ENV -Value "ASSET=$asset"

      - name: Upload artifact (for manual runs / debugging)
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.short }}
          path: |
            ${{ env.ASSET }}
            ${{ env.ASSET }}.sha256

      - name: Attach to GitHub Release
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            ${{ env.ASSET }}
            ${{ env.ASSET }}.sha256
          fail_on_unmatched_files: true
          # Exactly one leg of the matrix asks for the notes. This step runs
          # once per target, and the action appends the generated notes to
          # the body every time it runs against an existing release — so
          # asking on all five printed "What's Changed" five times over. The
          # v0.9.7 body carries four copies of it.
          generate_release_notes: ${{ matrix.short == 'x86_64-linux' }}
          # A tag with a SemVer pre-release part (`v1.0.0-rc.1`) is a
          # pre-release, and saying so is what keeps it out of
          # `/releases/latest` — which is exactly where `install.sh`
          # resolves a bare install. Without this, publishing a candidate
          # hands it to everyone who pipes the script with no
          # `JWC_VERSION`, with nothing in the output to say it is one.
          prerelease: ${{ contains(github.ref_name, '-') }}

# The VS Code extension is NOT published here. It has its own workflow —
# .github/workflows/vscode-marketplace.yml — which fires on the same `v*`
# tag and additionally verifies the extension identifier and the icon,
# publishes to OpenVSX, and degrades gracefully when a token is missing.
#
# This file used to carry a second `publish-vscode` job doing the same
# thing. Both raced on every tag; the marketplace workflow is standalone
# while this one waits on the build matrix, so it always lost and got
# `jwc-extension.jwc-lang v0.8.4 already exists.` — a red X on an otherwise
# complete release. The .vsix on the GitHub Release comes from the other
# workflow too, so nothing is lost by removing it.
